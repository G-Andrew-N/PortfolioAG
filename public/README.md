# Static assets

Place your profile photo here as **`profile.jpg`** (or rename your image to `profile.jpg`). It will appear in the Hero section on the homepage.

Supported formats: JPG, PNG, WebP. For best results use a square or portrait image (e.g. 800×800 or 600×800).
.